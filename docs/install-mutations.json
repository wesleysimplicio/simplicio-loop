{
  "consent_matrix": [
    {
      "effect": "global_package",
      "required_consent": "scope/mode itself is the explicit choice (no separate flag)",
      "trigger": "`scope in (user, system)` or `mode == full-stack`"
    },
    {
      "effect": "break_system_packages",
      "required_consent": "`--allow-break-system-packages` / `SIMPLICIO_ALLOW_BREAK_SYSTEM_PACKAGES=1` — **never applied unconditionally**",
      "trigger": "pip refuses with PEP 668's `externally-managed-environment`"
    },
    {
      "effect": "path_write / symlink",
      "required_consent": "implied by `--global`",
      "trigger": "`scope != project`"
    },
    {
      "effect": "service",
      "required_consent": "explicit flag; **`mode == full-stack` alone is NOT enough** — full-stack additionally requires `--with-service` **and** `--with-proxy` together, or the plan stays `BLOCKED` (`blocked_reasons: [\"full_stack_confirmation\"]`, #293 fix: mode name is never itself treated as consent)",
      "trigger": "`--with-service` or `mode == full-stack`"
    },
    {
      "effect": "proxy",
      "required_consent": "same as `service` above",
      "trigger": "`--with-proxy` or `mode == full-stack`"
    }
  ],
  "mutations": [
    {
      "consent_required": "no (default-mode effect)",
      "effect": "copies the 7 skills into `<target>/.claude/skills/<skill>`",
      "function": "copy_skills()",
      "reversible": "yes (delete dir)",
      "scope": "project/user",
      "source": "install_lib.py"
    },
    {
      "consent_required": "no",
      "effect": "copies `hooks/` into `<target>/hooks` (project) or `<target>/.claude/hooks` (global)",
      "function": "copy_hooks()",
      "reversible": "yes",
      "scope": "project/user",
      "source": "install_lib.py"
    },
    {
      "consent_required": "no",
      "effect": "copies `scripts/*.py` (minus `__pycache__`/`*.pyc`) into `<target>/scripts` (project) or `<target>/.claude/scripts` (global)",
      "function": "copy_scripts()",
      "reversible": "yes",
      "scope": "project/user",
      "source": "install_lib.py"
    },
    {
      "consent_required": "no",
      "effect": "creates/updates the runtime's entry file (`AGENTS.md`, `GEMINI.md`, `.github/copilot-instructions.md`, `.kiro/steering/simplicio-loop.md`, `CONVENTIONS.md`) between `<!-- simplicio-loop:begin/end -->` markers",
      "function": "ensure_entry()",
      "reversible": "yes (marker-delimited block is removable without touching the rest of the file)",
      "scope": "project/user",
      "source": "install_lib.py"
    },
    {
      "consent_required": "no",
      "effect": "merges `Stop` (+ project-local `PreToolUse`) hook entries into `.claude/settings.json`",
      "function": "merge_claude_hooks()",
      "reversible": "yes (JSON merge; existing unrelated keys untouched)",
      "scope": "project/user",
      "source": "install_lib.py"
    },
    {
      "consent_required": "no — but a *foreign* existing hook is never overwritten (logged, not clobbered)",
      "effect": "writes `.git/hooks/pre-commit` (only if the target is a git repo and no foreign hook already lives there)",
      "function": "install_git_precommit_hook()",
      "reversible": "yes (file replace/delete)",
      "scope": "project only",
      "source": "install_lib.py"
    },
    {
      "consent_required": "**yes for `--break-system-packages`** — only attempted when pip's stderr specifically reports `externally-managed-environment` AND `--allow-break-system-packages` (or `SIMPLICIO_ALLOW_BREAK_SYSTEM_PACKAGES=1`) was explicitly passed (#293 §3 hardening, see below)",
      "effect": "`pip install -U simplicio-cli` (the 2 required operator binaries)",
      "function": "ensure_operators() / _pip_install()",
      "reversible": "**not reversible by this installer** (a real `pip uninstall` is a separate, manual step)",
      "scope": "**global** Python environment (whatever `sys.executable` resolves to — a venv if active, else the system/user Python)",
      "source": "install_lib.py"
    },
    {
      "consent_required": "no (best-effort, never fails the install)",
      "effect": "symlinks console-scripts (`simplicio-dev-cli`, `simplicio-mapper`, `simplicio-loop`) into `~/.local/bin` when a `--user` pip install dropped them off PATH",
      "function": "_link_console_script() / _link_operator_bins()",
      "reversible": "yes (remove symlink)",
      "scope": "user (`~/.local/bin`)",
      "source": "install_lib.py"
    },
    {
      "consent_required": "**yes** — only runs when `--with-service`/`--full-stack` consent is given (#293 fix: no longer runs by default)",
      "effect": "`pip install -U .[onnx]` / `simplicio-loop[onnx]` + tray dep (`pystray`+`pillow` or `rumps` on macOS)",
      "function": "install_all_deps()",
      "reversible": "not reversible by this installer",
      "scope": "global Python environment",
      "source": "install_lib.py"
    },
    {
      "consent_required": "**yes** — only in `--mode full-stack` with explicit `--with-service`/`--with-proxy`",
      "effect": "copies `engine/` (capture-proxy code) and `app/` (tray code) into the target — the file surface of `full-stack` mode",
      "function": "copy_full_stack()",
      "reversible": "yes (delete dir)",
      "scope": "project/user",
      "source": "install_lib.py"
    },
    {
      "consent_required": "**yes** — requires `--with-service` (or `--full-stack`); OFF by default (#293 fix: was previously default-on, gated only by opt-out `--minimal`)",
      "effect": "registers the always-on capture proxy (`install_services.py install`/`wire` on Linux/Windows, `setup_simplicio.sh` — launchd — on macOS) + opens the Token Monitor dashboard once",
      "function": "setup_monitor()",
      "reversible": "services: yes (`install_services.py uninstall`); dashboard open: not applicable (no persistent mutation)",
      "scope": "system service scope",
      "source": "install_lib.py"
    },
    {
      "consent_required": "same as `setup_monitor()` above",
      "effect": "Linux: writes a `systemd --user` unit; Windows: registers a Startup-folder shim; wiring: edits provider base-URL env for Claude/Codex/Simplicio Agent",
      "function": "install() / wire()",
      "reversible": "yes (`uninstall()`)",
      "scope": "user/system, OS-specific",
      "source": "install_services.py"
    },
    {
      "consent_required": "governed entirely by the plan's `permissions_required` (see `install_plan.py`)",
      "effect": "wraps every one of the above FILE effects (skills/hooks/scripts/entry/claude_settings, + `engine`/`app` in full-stack mode) with a pre-mutation backup + before/after hash + persisted receipt under `<target>/.simplicio/receipts/<id>.json`; automatic rollback of every already-applied step if a later step raises",
      "function": "apply()",
      "reversible": "yes, byte-for-byte via `rollback()`",
      "scope": "project/user",
      "source": "install_executor.py"
    },
    {
      "consent_required": "no (this only ever removes paths the transaction's OWN prior manifest claims responsibility for)",
      "effect": "removes a skill directory that a PRIOR install's manifest recorded but the CURRENT release no longer declares (an N-1 → N upgrade cleanup)",
      "function": "manifest reconciliation (_stale_skills() + the reconcile step)",
      "reversible": "yes (same backup/restore mechanism as every other step)",
      "scope": "project/user",
      "source": "install_executor.py"
    }
  ],
  "os_differences": [
    {
      "concern": "Global install target",
      "linux": "`HOME` (from `os.path.expanduser(\"~\")`)",
      "macos": "`HOME`",
      "windows": "`HOME` (`%USERPROFILE%`)"
    },
    {
      "concern": "`~/.local/bin` symlink target",
      "linux": "real symlink (`os.symlink`)",
      "macos": "real symlink",
      "windows": "`os.symlink` requires Developer Mode or admin — falls back silently (best-effort; `_link_console_script` swallows `OSError`)"
    },
    {
      "concern": "`chmod +x` on `.git/hooks/pre-commit`",
      "linux": "applied (`0o755`)",
      "macos": "applied",
      "windows": "`os.chmod` is a no-op on most Windows filesystems; git only needs the file to be invocable via `sh` (`#!/usr/bin/env sh` shebang), which the file already has — the failed `chmod` is caught and logged as non-fatal"
    },
    {
      "concern": "Externally-managed Python (PEP 668)",
      "linux": "common on Debian/Ubuntu system Python",
      "macos": "common on Homebrew Python",
      "windows": "rare (python.org/Store installs are not externally-managed) — the detection in `_is_externally_managed_error()` still applies uniformly; it simply never fires here"
    },
    {
      "concern": "Capture-proxy service registration",
      "linux": "`install_services.py` → `systemd --user` unit",
      "macos": "`setup_simplicio.sh` → `launchd` agent",
      "windows": "`install_services.py` → Startup-folder shim"
    },
    {
      "concern": "Path separators in receipts/manifest",
      "linux": "POSIX (`/`)",
      "macos": "POSIX (`/`)",
      "windows": "`os.path` mixed; hashing walks (`_hash_path`) normalize relative paths to `/` before hashing so a receipt/manifest hash is stable across OSes for the same tree content"
    },
    {
      "concern": "Console-script extension",
      "linux": "none",
      "macos": "none",
      "windows": "`.exe` (pip) or `.CMD`/`.EXE` shims — `install_smoke.py`'s clean-room check and `_link_console_script()` both branch on `os.name == \"nt\"`"
    }
  ],
  "schema": "simplicio.install-mutations/v1"
}
