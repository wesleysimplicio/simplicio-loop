# Copilot / VS Code — Simplicio loop

Read `docs/ECOSYSTEM_LLM_GUIDE.md` and `AGENTS.md`.

- Runtime owns loop activation (`simplicio loop decide`) and execution metrics.
- mapper / dev-cli / fast work alone without Runtime.
- Prefer MCP tools when Runtime is registered.
- STRICT: no host hand-edit as primary mutation path.
