# Grok / xAI host rule

Grok is self-paced. Do not store xAI credentials in this adapter. Live API
calls are opt-in (`SIMPLICIO_GROK_LIVE=1`); otherwise live support stays
UNVERIFIED. Unknown tool calls must not fall back to shell.
Adapter: `adapters/grok/adapter.py`.
