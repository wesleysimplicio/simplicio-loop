System: PAGE-101
Feature: Responsive Simplicio landing page
Type: creation
Target: site/index.html

1. Acceptance Criteria

Scenario 1: Create the landing page (AC01)
Given the isolated fixture has no `site/index.html`
When the creation task is executed
Then `site/index.html` exists as a complete responsive HTML document and contains `Simplicio` [RN01]

Scenario 2: Preserve semantic structure (AC02)
Given the page is opened without JavaScript
When an independent filesystem verifier inspects it
Then it finds `<!doctype html>`, `lang="en"`, a viewport meta tag, a header, a main element, and a footer [RN02]

2. Business Rules

RN01 – Simplicio is the product identity and must be visible in the landing-page title or primary heading.
RN02 – The initial page must be usable without JavaScript.

3. Non-Functional Requirements

- NFR01 – Keep the result self-contained, accessible, and free of external runtime dependencies.

5. Access

- Local isolated benchmark fixture only; no network or deployment is required.

8. Additional Information

- Independent verifier: `python3 bench/verify_site_tasks.py --task PAGE-101 --repo .`
- Identifiers: AC01, AC02, NFR01, RN01, RN02
- Filesystem result: `site/index.html` exists and contains the required HTML, identity, viewport, semantic structure, and responsive stylesheet evidence after PAGE-103.

System: PAGE-102
Feature: Main navigation for the landing page
Type: editing
Target: site/index.html

1. Acceptance Criteria

Scenario 1: Add primary navigation (AC01)
Given `site/index.html` already exists from PAGE-101
When the editing task is executed
Then the existing page contains one semantic primary navigation landmark [RN02]

Scenario 2: Link the required destinations (AC02)
Given a visitor can see the primary navigation
When the independent filesystem verifier inspects its links
Then it finds `Home` to `index.html`, `About` to `about.html`, and `Contact` to `#contact` [RN01]

2. Business Rules

RN01 – Visitors must be able to reach the home, about, and contact destinations from the primary navigation.
RN02 – Navigation labels must be understandable without relying on icons or JavaScript.

3. Non-Functional Requirements

- NFR02 – Preserve the PAGE-101 landing-page content and responsive behavior while adding navigation.

5. Access

- Local isolated benchmark fixture only; no network or deployment is required.

8. Additional Information

- Independent verifier: `python3 bench/verify_site_tasks.py --task PAGE-102 --repo .`
- Identifiers: AC01, AC02, NFR02, RN01, RN02
- Filesystem result: `site/index.html` contains one primary navigation landmark with the three required links and destinations.

System: PAGE-103
Feature: External responsive stylesheet
Type: creation
Target: site/styles.css

1. Acceptance Criteria

Scenario 1: Create shared styles (AC01)
Given the landing page exists
When the creation task is executed
Then `site/styles.css` exists with readable base styles for the page and navigation [RN01]

Scenario 2: Link responsive styles (AC02)
Given `site/styles.css` exists
When the independent filesystem verifier inspects the fixture
Then `site/index.html` links the stylesheet and the stylesheet contains a media query for narrow viewports [RN02]

2. Business Rules

RN01 – Shared presentation rules belong in the external stylesheet instead of requiring JavaScript.
RN02 – The landing page must remain legible and navigable on narrow screens.

3. Non-Functional Requirements

- NFR03 – Use only local assets and standards-based CSS; do not add a build step.

5. Access

- Local isolated benchmark fixture only; no network or deployment is required.

8. Additional Information

- Independent verifier: `python3 bench/verify_site_tasks.py --task PAGE-103 --repo .`
- Identifiers: AC01, AC02, NFR03, RN01, RN02
- Filesystem result: `site/styles.css` exists, is linked from `site/index.html`, and contains a responsive media query.

System: PAGE-104
Feature: Accessible contact form
Type: editing
Target: site/index.html

1. Acceptance Criteria

Scenario 1: Add the contact section (AC01)
Given the landing page has navigation with a `#contact` destination
When the editing task is executed
Then `site/index.html` contains a contact section with `id="contact"` and a form [RN01]

Scenario 2: Make the form accessible (AC02)
Given a visitor uses the contact form without JavaScript
When the independent filesystem verifier inspects it
Then name, email, and message controls have associated labels, required fields, native control semantics, and a submit button [RN02]

2. Business Rules

RN01 – Contact submissions must request the visitor's name, email address, and message.
RN02 – Every form control must remain understandable and operable with labels and native HTML semantics.

3. Non-Functional Requirements

- NFR04 – Preserve the PAGE-102 navigation destination `#contact` and keep the form usable on narrow screens.

5. Access

- Local isolated benchmark fixture only; no network or deployment is required.

8. Additional Information

- Independent verifier: `python3 bench/verify_site_tasks.py --task PAGE-104 --repo .`
- Identifiers: AC01, AC02, NFR04, RN01, RN02
- Filesystem result: `site/index.html` contains the contact anchor, labeled name/email/message controls, required attributes, and a submit button.

System: PAGE-105
Feature: About page
Type: creation
Target: site/about.html

1. Acceptance Criteria

Scenario 1: Create the about page (AC01)
Given the isolated fixture has no `site/about.html`
When the creation task is executed
Then `site/about.html` exists as a complete HTML document with a meaningful title and heading [RN01]

Scenario 2: Preserve identity and return path (AC02)
Given a visitor opens the about page without JavaScript
When the independent filesystem verifier inspects it
Then the page visibly identifies `Simplicio` and contains a visible link to `index.html` [RN02]

2. Business Rules

RN01 – The about page must maintain Simplicio identity consistently with the landing page.
RN02 – Visitors must be able to return to the landing page using a visible link.

3. Non-Functional Requirements

- NFR05 – Use semantic HTML and preserve the local-only nature of the fixture.

5. Access

- Local isolated benchmark fixture only; no network or deployment is required.

8. Additional Information

- Independent verifier: `python3 bench/verify_site_tasks.py --task PAGE-105 --repo .`
- Identifiers: AC01, AC02, NFR05, RN01, RN02
- Filesystem result: `site/about.html` exists with a Simplicio title or heading and a link whose destination is `index.html`.
