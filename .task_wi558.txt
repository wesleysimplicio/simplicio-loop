Extend scripts/release_manifest.py with two fail-closed schema validators and a release-train check entrypoint.

1. Add function validate_component_release(schema: dict) -> (bool, list[str]):
   - Required top-level keys: component, repository, package, version, artifacts, compatibility_range, breaking_change (bool), changelog.
   - version must match semantic VERSION_RE (reuse existing regex).
   - artifacts must be a non-empty list; each item must have digest (non-empty str), size (int>=0), signature (non-empty str).
   - compatibility_range must be a non-empty str.
   - changelog must be a non-empty str.
   - Return (False, errors) on any violation; (True, []) when valid.

2. Add function validate_ecosystem_release(schema: dict) -> (bool, list[str]):
   - Required top-level keys: release_id (str, monotonic-ish non-empty), components (dict non-empty mapping component->version/commit/digest), graph_hash (str), contract_hashes (dict), status (dict), evidence (list), rollout (dict).
   - components must be a non-empty dict.
   - Return (False, errors) on any violation; (True, []) when valid.

3. Add function release_train_check(repo: str = ".") -> int:
   - Load fixtures from <repo>/tests/fixtures/release_train/component_release_ok.json and ecosystem_release_ok.json if present.
   - Validate each with the validators above; collect errors.
   - Run build_manifest(Path(repo).resolve()) and include its errors/mismatches.
   - Print a structured JSON summary {"ready": bool, "schema_errors": [...], "manifest": {...}}.
   - Return 0 if all schemas valid AND manifest ready, else 1.

4. In simplicio_loop/cli.py:
   - Add subparser: p_rt = sub.add_parser("release-train", help="release train continuous verification")
   - Add nested subparser: rt_sub = p_rt.add_subparsers(dest="release_train_command", required=True); p_rt_check = rt_sub.add_parser("check", help="validate component/ecosystem release schemas + local drift")
   - p_rt_check.add_argument("--repo", default=".")
   - In main dispatch add: if command == "release-train": return release_train_check(args.repo)
   - Import release_train_check from scripts.release_manifest at top (add to existing import if any, else new import line near other script imports).

Do NOT modify test files. Do NOT change build_manifest. Return the diff.
